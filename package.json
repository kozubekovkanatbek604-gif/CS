{
  "name": "cyber-book-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "fly:scale-one": "fly scale count 1 --yes -a cs-cybersecurity-website",
    "fly:deploy": "fly deploy --ha=false -a cs-cybersecurity-website"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
